# Contract 1.3 Errata Resolution

The published `1.3.0-contract` snapshot remains byte-for-byte immutable. Contract `1.4.0-contract` records compatible runtime constraints with `x-runtime-enum` or `x-runtime-unsupported`; schema-narrowing removals are reserved for `/api/v2` so existing 1.3 clients are not silently broken.

| Errata ID | 1.3 definition                                                                               | Current backend behavior                                                                                                                                                                                               | Type                                 | Client compatibility impact                                                                   | Resolution                                                                                           | Breaking                                                 | Target                    | Test evidence                                                             | Final status                       |
| --------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------- | ---------------------------------- |
| ERR-01    | `listStudents.sort` is an open string.                                                       | Accepts `fullName`, `-fullName`, `studentNumber`, `-studentNumber`, `createdAt`, `-createdAt`.                                                                                                                         | Contract too broad                   | Other values already fail validation.                                                         | Retain the string and publish the exact `x-runtime-enum`; reserve schema narrowing for V2.           | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and compatibility fixtures                                    | RESOLVED_COMPATIBLY                |
| ERR-02    | `listEnrollments.sort` is an open string.                                                    | Accepts `joinedAt`, `-joinedAt`.                                                                                                                                                                                       | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and enrollment E2E                                            | RESOLVED_COMPATIBLY                |
| ERR-03    | `listRosterImports.sort` is an open string.                                                  | Accepts `versionNumber`, `-versionNumber`.                                                                                                                                                                             | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and roster E2E                                                | RESOLVED_COMPATIBLY                |
| ERR-04    | `listRosterEntries.sort` is an open string.                                                  | Accepts `sourceRowNumber`, `-sourceRowNumber`.                                                                                                                                                                         | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and roster E2E                                                | RESOLVED_COMPATIBLY                |
| ERR-05    | `listRosterAlignmentResults.sort` is an open string.                                         | Accepts `createdAt`, `-createdAt`.                                                                                                                                                                                     | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and roster E2E                                                | RESOLVED_COMPATIBLY                |
| ERR-06    | `listExerciseRecords.sort` is an open string.                                                | Accepts `businessDate`, `-businessDate`.                                                                                                                                                                               | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and record E2E                                                | RESOLVED_COMPATIBLY                |
| ERR-07    | `listExerciseRecordReviews.sort` is an open string.                                          | Accepts `reviewVersion`, `-reviewVersion`.                                                                                                                                                                             | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and review E2E                                                | RESOLVED_COMPATIBLY                |
| ERR-08    | `listScoreRules.sort` is declared without semantics.                                         | Uses fixed `-ruleVersion` order and does not expose user-selected sorting.                                                                                                                                             | Contract-only field                  | Existing calls remain accepted; the value cannot alter ordering.                              | Mark the field deprecated and `x-runtime-unsupported`; approve a new sortable vocabulary only in V2. | Removal or semantic activation would be breaking.        | 1.4 metadata; V2 decision | Parity gate and score E2E                                                 | RESOLVED_BY_EXPLICIT_COMPATIBILITY |
| ERR-09    | `listStudentScores.sort` is declared without semantics.                                      | Uses fixed `-updatedAt` order.                                                                                                                                                                                         | Contract-only field                  | Existing calls remain accepted; the value cannot alter ordering.                              | Mark deprecated and `x-runtime-unsupported`; retain deterministic ordering.                          | Removal or semantic activation would be breaking.        | 1.4 metadata; V2 decision | Parity gate and score E2E                                                 | RESOLVED_BY_EXPLICIT_COMPATIBILITY |
| ERR-10    | `listScoreAdjustments.sort` is declared without semantics.                                   | Uses fixed `-requestedAt` order.                                                                                                                                                                                       | Contract-only field                  | Existing calls remain accepted; the value cannot alter ordering.                              | Mark deprecated and `x-runtime-unsupported`; retain deterministic ordering.                          | Removal or semantic activation would be breaking.        | 1.4 metadata; V2 decision | Parity gate and score E2E                                                 | RESOLVED_BY_EXPLICIT_COMPATIBILITY |
| ERR-11    | `listExports.sort` is an open string.                                                        | The operation is intentionally disabled; validated vocabulary is `requestedAt`, `-requestedAt`.                                                                                                                        | Contract too broad on disabled route | Calls still fail closed with `SYSTEM_MODE_UNSUPPORTED`.                                       | Publish `x-runtime-enum` without enabling Export.                                                    | No in 1.4.                                               | 1.4                       | Parity gate and Export fail-closed E2E                                    | RESOLVED_COMPATIBLY                |
| ERR-12    | `listAuditLogs.sort` is an open string.                                                      | Accepts `occurredAt`, `-occurredAt`.                                                                                                                                                                                   | Contract too broad                   | Other values already fail validation.                                                         | Publish endpoint-specific `x-runtime-enum`; retain 1.3 input shape.                                  | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and audit E2E                                                 | RESOLVED_COMPATIBLY                |
| ERR-13    | `listClassSections.status` is an open string.                                                | Accepts `UPCOMING`, `ACTIVE`, `CLOSED`, `ARCHIVED`.                                                                                                                                                                    | Contract too broad                   | Invented values already fail validation.                                                      | Publish the canonical `x-runtime-enum`; defer closed-schema narrowing to V2.                         | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and teaching E2E                                              | RESOLVED_COMPATIBLY                |
| ERR-14    | Upload `captureSource` reuses the broader enum including `SYSTEM_IMPORT`.                    | Student upload accepts only `IN_APP_CAMERA` and `FILE_PICKER`.                                                                                                                                                         | Shared enum too broad                | `SYSTEM_IMPORT` remains rejected as documented.                                               | Publish request-specific `x-runtime-enum`; retain the shared response enum.                          | No in 1.4; closed request enum in V2 is breaking.        | 1.4 metadata; V2 schema   | Parity gate, media unit/E2E                                               | RESOLVED_COMPATIBLY                |
| ERR-15    | Media access `purpose` is an open uppercase token.                                           | Accepts only `VIEW_ORIGINAL`.                                                                                                                                                                                          | Unsafe open vocabulary               | Other tokens already fail validation.                                                         | Publish `x-runtime-enum: [VIEW_ORIGINAL]`; reserve schema narrowing for V2.                          | No in 1.4; V2 narrowing is breaking.                     | 1.4 metadata; V2 schema   | Parity gate and media E2E                                                 | RESOLVED_COMPATIBLY                |
| ERR-16    | `listStudentScores.status` does not define precedence across revision and publication facts. | Implements one mutually exclusive aggregate Score status: no revision=`NOT_CALCULATED`; unpublished current revision=`CALCULATED`/`ADJUSTED`; equal working/published pointers=`PUBLISHED`; V1 has no lock transition. | Semantic ambiguity                   | The existing enum and query name are retained; the filter now produces deterministic subsets. | Define the exact semantics in 1.4 and enforce them in the Prisma predicate and projection.           | No request-shape break; behavior is a defect correction. | 1.4                       | Unit projection, PostgreSQL integration, strict E2E, contract description | RESOLVED                           |

All 16 records have a final disposition. No unresolved Contract 1.3 exception remains in the parity gate.
