# 后端统一契约冲突矩阵

> 状态：阶段 0 基线；后续阶段发现新证据时持续更新。
> “缺失”表示当前工作区没有该客户端/服务端实现，不能据文档推定其行为。

| 编号 | 业务概念 | Android | iOS | Web 学生端 | 教师端 | 管理端 | 后端/数据库 | 冲突说明 | 推荐处理 |
|---|---|---|---|---|---|---|---|---|---|
| C-001 | 后端实现基线 | 有 API client，无服务端源码 | 缺失 | 缺失 | Mock/内存写入 | Mock/浏览器状态 | 当前工作区缺失 | `CLAUDE.md` 声称后端完整，但文件树无后端/SQL/OpenAPI | 阶段 10 前确认唯一权威后端仓库；此前只冻结合同 |
| C-002 | 学生端覆盖 | 已实现 Kotlin App | 文档声明，源码缺失 | 文档声明，源码缺失 | 不适用 | 不适用 | 无法验证 | 文档把三端视为已覆盖，实际只有 Android | iOS/Web 学生端标记“尚未实现”，不得假定字段一致 |
| C-003 | 课程加入 | 当前已改为扫码/邀请码直接加入 | 缺失 | 缺失 | 无审批入口 | 无审批职责 | 服务端事务缺失 | 旧审批模型已从主要 UI 移除，但真实幂等/唯一约束无法验证 | 统一为直接创建 `ACTIVE Enrollment`；旧审批 API 只做兼容审计，不进入正常状态机 |
| C-004 | User/Profile/Enrollment | `StudentProfile`、token、membership 混在 workspace | 缺失 | 缺失 | `Student` 同时含身份与入班字段 | `AdminUser` 同时含账户/身份 | schema 缺失 | 身份、账户和课程关系边界不清 | 分为 `User`、各 Profile、`Enrollment` |
| C-005 | Course/ClassSection | `Course` 同时含 code/section/teacher/semester | 缺失 | 缺失 | `Course` 实为具体教学班 | 学期下直接统计课程 | schema 缺失 | 课程定义与开课实例混用 | 分为 `Course` 与 `ClassSection` |
| C-006 | 内部 ID 与学号 | String `id/studentId/studentNumber`；Mock 中相等 | 缺失 | 缺失 | number 型内部 id + `number` 学号 | `account` 常当学号 | schema 缺失 | 名称、类型和语义均不一致 | 内部 `id` 使用 opaque UUID/string；学校学号只用 `studentNumber` string |
| C-007 | 记录 ID | Android `id: String` | 缺失 | 缺失 | `recordId: number` | 仅展示字符串资源 ID | schema 缺失 | 同一业务标识类型不一致 | `recordId` 统一 opaque string；全链路保留同一值 |
| C-008 | API 前缀 | `/auth`、`/sport`、`/student`、`/v1` 混用 | 缺失 | 缺失 | 名单声明 `/api/v1/teacher` | 无真实 API | 未提供 | 版本位置和资源风格碎片化 | 新合同统一 `/api/v1`；旧路径设兼容适配与移除版本 |
| C-009 | API 响应 | DTO 各自裸结构且默认值多 | 缺失 | 缺失 | 本地对象 | 本地对象 | 未提供 | 无统一 envelope、分页、错误结构 | 统一 `data/meta` 成功结构与 `code/message/details/requestId/timestamp` 错误结构 |
| C-010 | 时长单位 | 会话毫秒、请求秒+小时、概览 `Double` 小时 | 缺失 | 缺失 | 分钟 + 小时 | 小时/分钟 | 未提供 | 同一事实多单位且可能舍入不同 | 事实字段统一整数秒；小时/分钟仅展示层派生 |
| C-011 | 1h/2h 边界 | `>=3600s =>1`、`>=7200s =>2` | 缺失 | 缺失 | Mock 以分钟/小时计算 | 不参与 | 无法验证 | Android边界清晰，其他端/服务端未证实 | 后端固定 `[0,3600)=0`、`[3600,7200)=3600`、`>=7200=7200` credited seconds |
| C-012 | 总学时目标 | DTO 默认 20h，但业务文档说教师双目标可配置 | 缺失 | 缺失 | 课程相关/其他目标可配置 | 文档说管理员不配置 | 无法验证 | 本次指令要求累计 20h 触发成绩，与当前分类配置冲突 | 阶段 4 已按 ADR-061 固定总门槛 72000 秒；分类分配/配置职责由 ADR-062 继续阻塞，公式由 ADR-018 阻塞 |
| C-013 | 打卡审核 | UI 不展示审核流程；记录默认计入 | 缺失 | 缺失 | 文档说期末集中复核；代码又有 `pending/valid/invalid` | 不审核 | 未提供 Review 表 | 当前文档、Web UI 和本次 `ReviewResult` 基线互相冲突 | 阶段 4 冻结：新提交先 PENDING，只有最新 Review=`VALID` 贡献；INVALID 为 0 且不删原记录，客户端必须迁移 |
| C-014 | 审核历史 | 仅当前反馈字段 | 缺失 | 缺失 | React state 覆盖当前结果 | 审计为演示数据 | 未提供 | 无 append-only 历史与并发版本 | 新建 `ReviewRecord` 历史；修改结果新增一条记录并审计 |
| C-015 | 名单对齐对象 | 无 | 缺失 | 缺失 | 独立 Mock 类型/算法 | 可见全局数据但无真实操作 | 无持久化 | UI 已实现但服务端、文件安全、版本和任务均缺失 | 保持 Import/Entry/Result/Resolution 分离，后端实现前不得称上线 |
| C-016 | 名单状态 | 无 | 缺失 | 缺失 | `INFO_MISMATCH/POSSIBLE_MATCH/NOT_JOINED/.../DUPLICATE` | 无 | 未提供 | 与统一基线 `IDENTITY_CONFLICT/MISSING_IN_PLATFORM/.../DUPLICATED` 不同 | 建立明确映射；需要人工确认的语义不得自动合并 |
| C-017 | 会话与记录 | 本地 sealed session，提交后形成 record | 缺失 | 缺失 | 只见最终 record | 不参与 | 未提供 | 服务端没有可验证的 session、防双设备或恢复事实 | 阶段 4 按 ADR-063 冻结服务端时间优先和单活动 session；离线/心跳具体阈值仍由 ADR-021 阻塞 |
| C-018 | 媒体上传 | multipart `/upload/proof`，响应含 URL/cosKey | 缺失 | 缺失 | 文件名数组 Mock | 无对象存储 | 未提供 | 存储实现字段暴露；无上传确认/绑定状态 | 申请上传→对象存储→确认→`mediaId`→提交绑定；私有短链访问 |
| C-019 | 媒体数量/来源 | 6 图+1视频、App 相机、本地草稿 | 缺失 | 缺失 | Mock 未校验真实来源 | 不参与 | 未提供 | 客户端规则存在，后端强制校验无法证实 | ADR-066 冻结打卡用途 1..6 图、0..1 视频、至少一项且 IN_APP_CAMERA；跨业务用途继续由 ADR-030 决定 |
| C-020 | 成绩模型 | 仅展示字段，规则 DTO 默认值 | 缺失 | 缺失 | `published` boolean + 耐力成绩 | 维护换算表/修正窗口 Mock | 未提供 | 计算、人工调整、发布、锁定与来源未分离 | `ScoreRule` 版本、`StudentScore`、`ScoreAdjustment` 分离并可追溯 |
| C-021 | 权限 | token + 本地按钮/模式 | 缺失 | 缺失 | 角色路由/按钮 | 角色路由/按钮 | 中间件缺失 | UI 隐藏不构成后端授权 | RBAC 后再校验组织、教学班、本人、资源状态 |
| C-022 | 系统模式 | `NORMAL/READ_ONLY/MAINTENANCE` 可控制客户端写入口 | 缺失 | 缺失 | 页面状态 Mock | 管理页面 Mock | 未提供 | 只能证明 UI，无法保证服务端拒绝写入 | 后端统一策略中间件；客户端只作提示 |
| C-023 | 错误判断 | 多数抛 HTTP body 文本 | 缺失 | 缺失 | toast/表单字符串 | toast/表单字符串 | 未提供 | 客户端可能依赖文本，缺稳定业务码 | 阶段 7 冻结错误码；业务分支只看 `code` |
| C-024 | 时间 | Android字段既有 ISO也有显示字符串；Web Mock 混本地日期字符串/ISO | 缺失 | 缺失 | 本地格式混用 | 本地格式混用 | 未提供 | 时间点、日期和时区不一致 | DB UTC；API RFC3339/ISO8601 带时区；日期单独 `YYYY-MM-DD` |
| C-025 | 数据库字段暴露 | `cosKey` 直接进入 Android DTO | 缺失 | 缺失 | UI 模型近似表字段 | UI 模型近似表字段 | schema 缺失 | 客户端耦合存储/表结构 | API 使用业务资源 DTO，隐藏 storage key、内部备注等敏感字段 |
| C-026 | Mock 与真实模型 | `studentId == studentNumber`、中文状态、默认 20h | 缺失 | 缺失 | 数字 ID/中文业务状态 | 大量模拟账号/审计 | 无法核对 | Mock 可掩盖 ID/枚举/权限错误 | 从 OpenAPI 生成统一 fixture；Mock 明示、不得回退成真实成功 |
| C-027 | 管理员组织范围 | 不适用 | 缺失 | 缺失 | 教师仅本人课程（目标） | 文档写全部数据 | schema 缺失 | 本次要求管理员只管理所属组织；当前只有单校假设 | 建模 `Organization`；当前固定单 BNBU，仍按 organizationId 过滤 |
| C-028 | 删除策略 | 本地清理草稿 | 缺失 | 缺失 | 移出保留历史 | 文档含全量物理清理 | 无审计实现 | 物理删除与追溯要求冲突 | 核心事实禁止普通物理删除；高危清理单独运维流程待确认 |
| C-029 | 学生认证 | 遗留学号密码 DTO/调用，也有验证码登录 | 缺失 | 缺失 | 工号密码 | 工号密码 | 历史文档称 password/email 非空 | 新规则为学生无密码且先入班后绑定联系方式，与遗留合同/隐私政策冲突 | `User` 允许学生无 password；认证方式按角色分离；旧密码登录只做迁移兼容 |
| C-030 | 归档成绩修正 | 只读展示 | 缺失 | 缺失 | 文档一处要求管理员审批开窗 | 文档一处称管理员不参与、教师直接改 | 未提供 | 发起、批准、执行、关闭职责相互矛盾 | 列为阻塞决策；未批准前不开放归档成绩写 API |
| C-031 | 学期切换 | 检测新学期并清本地 workspace | 缺失 | 缺失 | 文档称旧学期是否归档另行决定 | 文档/Mock 自动将旧 current 归档 | 未提供 | 自动/人工归档语义不同，影响写锁定 | 冻结单一事务或显式两步流程后再实现 |
| C-032 | GPS | 客户端可单次定位；隐私政策称不上传 | 缺失 | 缺失 | 业务文档预期 90 天脱敏地图 | 管理端不直接使用 | 未提供 | 业务文档称上传保留 90 天，隐私政策称不上送 | 阻塞定位字段/地图；先完成用途、精度、同意与保留期决策 |
| C-033 | 附件来源 | 打卡 UI 走相机草稿；免测/反馈用途不同 | 缺失 | 缺失 | 查看凭证 | 不参与 | 未提供 | 业务规则禁止打卡相册，隐私政策宽泛允许照片选择器 | 按 `captureSource` + businessPurpose 分开白名单，不把免测规则套到打卡 |
| C-034 | 通知关闭 | App 内消息可强制生成，OS Push 可被用户关闭 | 缺失 | 缺失 | 业务操作触发通知 | 管理帮助/公告 | 未提供 | 文档“不可关闭”与操作系统权限现实冲突 | 定义“必须生成 App 内通知”；Push 为尽力投递并保留已读状态 |
| C-035 | 数据保留 | 本地草稿按会话清理 | 缺失 | 缺失 | 历史教学数据需查阅 | 管理文档允许全量清理 | 无归档系统 | 全量清理与六年/长期留存承诺冲突 | 先定记录类别保留表、归档库和 legal hold，再设计清理 |
| C-036 | 管理员代行教师职责 | 不适用 | 缺失 | 缺失 | 文档原则为本人教学班 | “授权/特殊情况”例外未定义 | 未提供 | 例外无授权主体、期限、范围或审计 | 默认禁止；若保留，单独设计临时授权资源并强审计 |
| C-037 | 登录与危险操作 | Android 有真实请求封装 | 缺失 | 缺失 | 任意非空凭证可登录 | 角色靠正则；删除/清理校验硬编码密码 | 无认证服务 | 演示行为可能被误认为安全实现 | 明示 Mock；所有认证/重认证在服务端完成，密码不得进入源码 |
| C-038 | 成绩公式来源 | 客户端存在默认规则/展示计算 | 缺失 | 缺失 | 硬编码换算函数 | 另有 Mock 区间表 | 无 ScoreRule | 两套公式对同一用时可能给不同分 | 后端版本化 ScoreRule 为唯一来源，未确认公式不实现 |
| C-039 | 归档写锁 | 客户端按服务端课程状态决定 | 缺失 | 缺失 | ENDED 课程仍可进入多项写操作 | 学期状态仅 Mock | 无后端拦截 | UI 与文档只读承诺不一致 | 后端统一状态守卫，归档后默认拒绝写入 |
| C-040 | Mock 身份一致性 | Android 有独立 Mock 学生 | 缺失 | 缺失 | 某些学号对应姓名 A | 同一学号对应姓名 B | 无 fixture 主源 | 跨页面联调会误连身份 | 从统一合同生成一套非真实测试 fixture，内部 ID/学号都唯一 |
| C-041 | 同一学生完成度 | Android概览依赖 summary | 缺失 | 缺失 | 列表累计来自预置字段，详情只累加 valid record 且减免口径不同 | 不参与 | 无聚合服务 | 同一学生在列表与详情可能显示不同结果 | 所有汇总由后端从有效记录、调整、减免和规则单源计算 |
| C-042 | 验证码会话 | UI 能发送/验证，但成功后未安装 token/workspace | 缺失 | 缺失 | Web 为假登录 | Web 为假登录 | 无认证服务 | “页面走完”不等于已建立认证会话 | 统一验证码 verify 响应并做端到端 session 测试 |
| C-043 | 每日一次 businessDate | 按 submittedAt + 设备默认时区 | 缺失 | 缺失 | Mock 按显示日期 | 不参与 | 无法验证 | 现有规则要求按运动开始日期，跨日/时区会漂移 | ADR-037/064：服务端按 organization timezone 从 startedAt 生成 immutable businessDate，并按 Enrollment+businessDate 强制每日一条成功提交 |
| C-044 | 不足 1h 草稿 | 实现清除媒体草稿 | 缺失 | 缺失 | 不涉及 | 不涉及 | 未提供 | 业务文档要求保留，代码相反 | ADR-065 冻结不足 3600 秒不形成正式 Record；本地媒体草稿保留期限仍由 ADR-040 阻塞 |
| C-045 | 2h 上限动作 | 达上限切 Paused | 缺失 | 缺失 | 只见最终记录 | 不涉及 | 未提供 | 旧文档称自动结束，本次基线称停止累计待确认 | ADR-041：新 session 到 7200 秒转 `COMPLETED` 并停止累计，Record 仍待学生确认提交 |
| C-046 | 内部备注 | 学生响应/模型包含 `teacherInternalNote` | 缺失 | 缺失 | 教师需读写内部备注 | 管理员无日常访问 | 未提供 | 学生 API 下发了不该暴露的字段 | 学生 projection 排除内部备注；教师端点按 scope 返回 |
| C-047 | 位置事实 | 已采集坐标但提交 DTO 丢弃 | 缺失 | 缺失 | 目标文档预期地图 | 不参与 | 未提供 | 采集、上传、保留与隐私声明不闭环 | ADR-029 决策前不新增位置 API；若采集须显式同意和状态字段 |
| C-048 | 安全枚举默认 | 未知账户状态→ACTIVE；未知系统模式→NORMAL | 缺失 | 缺失 | 无服务端状态 | 本地 Mock 状态 | 未提供 | 合同漂移时客户端错误开放写能力 | 权限相关未知值 fail closed，并上报兼容错误 |
| C-049 | 课程 membership ID | join 响应包含 membership id，安装 workspace 后只留课程状态 | 缺失 | 缺失 | 成员 ID 是数值 student/course 组合 | 无持久化 | 未提供 | `enrollmentId` 丢失，后续 record 无法可靠关联 | 将 response membership 映射为稳定 `Enrollment` 并贯穿 record |
| C-050 | CreditType wire value | Kotlin enum向 API发送中文 label | 缺失 | 缺失 | 中文标签/字符串 | 不参与 | 未提供 | 本地化文案被当业务枚举 | wire 只用稳定英文枚举，显示文本由 i18n key 映射 |
